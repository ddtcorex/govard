networks:
    govard-net:
        driver: bridge
    govard-proxy:
        external: true
services:
    web:
        command: |
            sh -lc 'if [ ! -d node_modules ] || [ -z "$$(ls -A node_modules 2>/dev/null)" ]; then npm install; fi; exec npm run dev -- --hostname 0.0.0.0 --port 80;'
        environment:
            - NODE_ENV=development
            - PORT=80
        extra_hosts:
            - host.docker.internal:host-gateway
            - mail:host-gateway
        image: node:24
        networks:
            - govard-net
            - govard-proxy
        user: root
        volumes:
            - .:/app
            - next-cache:/app/.next
        working_dir: /app
volumes:
    next-cache: null
